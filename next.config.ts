import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['assets.aceternity.com'], // Add the image domain here
  },
  // Add other configuration options as needed
};

export default nextConfig;
